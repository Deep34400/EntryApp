/**
 * Theme re-export for compatibility. Prefer importing from @/theme.
 */
export { Colors, Spacing, BorderRadius, Typography, Fonts } from "@/constants/theme";
export type { ThemeColors } from "@/constants/theme";
export { Layout } from "@/constants/theme";
