/**
 * Theme re-export for compatibility. Prefer importing from @/theme.
 */
export { colors as Colors, spacing as Spacing, radius as BorderRadius, typography as Typography, fontFamily as Fonts } from "@/theme";
export type { ThemeColors } from "@/theme";
export { layout as Layout } from "@/constants/layout";
