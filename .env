# Backend API URL — the app calls this URL for all API requests (e.g. POST /api/tickets).
# Use this to connect the frontend directly to your backend.

# Option A: Direct full URL (recommended for local or your own server)
EXPO_PUBLIC_API_URL=https://qrm792qn-8080.inc1.devtunnels.ms/

# Option B: For Replit — set in package.json "expo:dev" script instead:
# EXPO_PUBLIC_DOMAIN=$REPLIT_DEV_DOMAIN:5000

# After changing, restart the Expo app so it picks up the new value.
